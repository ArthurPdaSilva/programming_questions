import { Typography } from '@mui/material';

export default function GameTitle() {
  return (
    <Typography variant="h1" color="primary" fontWeight="bold" align="center">
      Programming Questions
    </Typography>
  );
}
